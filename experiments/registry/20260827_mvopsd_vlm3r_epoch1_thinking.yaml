experiment_id: 20260827_mvopsd_vlm3r_epoch1_thinking
title: "vlm3r 完整 1 epoch（477 步）：teacher thinking ON + 验证采样 + boxed"
status: running
parent: 20260826_mvopsd_vlm3r_epoch1_boxed_sample

created_at: "2026-08-27T21:05:00+08:00"
started_at: "2026-08-27T21:04:15+08:00"
owner: cursor-agent
node: node-A
driver_pid: 1478526

goal: >
  在 vlm3r_scannet 单源数据上完整重训 1 epoch，唯一相对 20260826 的控制变量是
  teacher_enable_thinking=True。对照 20260824 在 llava_hound 上的结论（thinking
  导致响应长度爆炸），观察 vlm3r 数据源上是否复现同类退化。

design:
  changed_vs_parent:
    - "actor_rollout_ref.actor.self_distillation.teacher_enable_thinking: False -> True"
  same_as_parent:
    - "数据：vlm3r_scannet 30,567 行，batch 64，477 步"
    - "验证：vsibench_val_boxed.parquet，采样 t=1.0 top_p=0.8"
    - "vsibench_boxed_primary=True"
  differs_from_parent_runtime_defaults:
  - "JUDGE=0：训练内 rule-only，WandB 不上报 judge 系列（用户 2026-08-27 决策）"
  - "val response_length=4096（项目默认已更新）"

command: |
  env -u PYTHONNOUSERSITE -u CONDA_PREFIX -u CONDA_DEFAULT_ENV -u CONDA_SHLVL \
      -u CONDA_PYTHON_EXE PATH=/usr/bin:/usr/local/bin:/bin \
    EXPERIMENT_NAME=20260827_mvopsd_vlm3r_epoch1_thinking \
    TRAIN_FILE=$PWD/data/mvopsd/parquet_single_vlm3r_scannet/main_train.parquet \
    VAL_FILE=$PWD/data/eval/vsibench_verl/vsibench_val_boxed.parquet \
    TOTAL_STEPS=477 TEST_FREQ=25 \
    VAL_DO_SAMPLE=1 VAL_TEMPERATURE=1.0 VAL_TOP_P=0.8 VAL_N=1 \
    JUDGE=0 \
    bash scripts/opsd/run_mvopsd.sh main \
      actor_rollout_ref.actor.self_distillation.teacher_enable_thinking=True

outputs:
  train_log: logs/train/20260827_mvopsd_vlm3r_epoch1_thinking/
  val_dump: logs/val/20260827_mvopsd_vlm3r_epoch1_thinking/
  rollouts: rollouts/20260827_mvopsd_vlm3r_epoch1_thinking/
  checkpoint: checkpoints/20260827_mvopsd_vlm3r_epoch1_thinking/global_step_250
  checkpoint_merged_hf: output/20260827_mvopsd_vlm3r_epoch1_thinking_global_step_250_hf

checkpoint_retention:
  at: "2026-08-29T01:10:00+08:00"
  kept: global_step_250
  deleted_fsdp: [25, 50, 75, 100, 125, 150, 175, 200, 225, 275, 300, 325, 350]
  deleted_merged_hf: [150, 175, 200, 225]
  note: "用户要求只保留 step 250；评测日志未删"

watch:
  - "response_length/mean 与 clip_ratio 是否在 step 25 内暴涨（LESSON-029）"
  - "val-core/vsibench/overall/acc（rule-only，WandB 主曲线）"
  - "teacher_probe 是否仍冻结不变"
